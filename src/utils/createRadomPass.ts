import bcrypt from "bcryptjs";

export default async function createRamdomPassword() {
//   const salt = await bcrypt.genSalt(10);
//   const hashPassword = await bcrypt.hash(req.body.password, salt);
}
